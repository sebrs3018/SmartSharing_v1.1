<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sebrs3018.passaggiodatitraactivities">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PassaggioDatiTraActivities">

        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Ogni activity nuova aggiunta deve essere aggiunta qua
            Da notare che in action diciamo al mio activity a COSA reagire (provenienza del nuovo action)
        -->
        <activity android:name=".AnotherActivity">
            <intent-filter>
                <action android:name="com.sebrs3018.passaggiodatitraactivities.AnotherActivity" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>


        <activity android:name=".AnotherActivity4Results">
            <intent-filter>
                <action android:name="com.sebrs3018.passaggiodatitraactivities.AnotherActivity4Results" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>



    </application>

</manifest>